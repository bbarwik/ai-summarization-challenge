services:
  ai-pipeline:
    image: mcr.microsoft.com/devcontainers/python:3.12-bookworm
    init: true
    command: /bin/sh -lc "trap 'exit 0' TERM; while :; do sleep 600; done"
    working_dir: /workspaces/ai-pipeline
    user: vscode
    volumes:
      - ..:/workspaces/ai-pipeline:cached
